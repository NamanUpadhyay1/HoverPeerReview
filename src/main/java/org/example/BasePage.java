package org.example;

import org.openqa.selenium.WebDriver;

public class BasePage
    // The initial page of POM structure which will consist of drivers.
{
    protected static WebDriver driver;
    // WebDriver to use browser functions/actions.
}